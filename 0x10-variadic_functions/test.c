#include <stdio.h>

/**
 * main - entry point
 * Return: Always 0 
 */
int main(void)
{
int i;

for (i = 0; i <= 0; i++)
{
if (0 == NULL)
printf("Zero is null\n");
else
printf("Zero is not null");
}

return (0);
}
